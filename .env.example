# Quant Lab V0.1 is research + paper trading only.
# Do not put brokerage credentials in this file.
# Future broker integrations will read secrets from the environment,
# never from the UI or strategy layer.

QUANTLAB_ENV=research
QUANTLAB_CONFIG=config/default.yaml
QUANTLAB_DB_PATH=data/quantlab.db
QUANTLAB_LOG_LEVEL=INFO
