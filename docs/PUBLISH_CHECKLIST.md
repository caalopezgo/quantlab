# Optional: public demo

The GitHub repo is already public: https://github.com/caalopezgo/quantlab

To add a live Streamlit demo:

1. Open https://share.streamlit.io  
2. New app → this repo → branch `main` → main file `app.py`  
3. Deploy (uses `requirements.txt`)

See also [`DEPLOY.md`](DEPLOY.md).
